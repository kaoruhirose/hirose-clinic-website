# ドメイン切り替え作業の引き継ぎメモ

## ゴール
ドメイン `hiroseshinryojo.jp` を「エックスサーバー」から「Vercel」に
向け直して、Vercel 上のサイトを本番ドメインで表示させる。

## 現状
- サイトのコードは GitHub にプッシュ済み（Next.js）。Vercel にデプロイ想定。
- ドメイン `hiroseshinryojo.jp` はエックスサーバーで管理中。
- 現在、A レコードは `85.131.209.115`（旧サーバー）を指している。
- これを Vercel 向けに変更する作業の途中。

## ⚠️ 重要な注意点（必ず守る）
- **メール用の MX レコードには絶対に触れない**（メールが止まる恐れ）。
- 変更するのは **A レコード**と **`www` の CNAME** だけ。
- 既存レコードを勝手に消さない。変更前に必ず内容を確認する。

## 次にやること（最初のステップ）
1. エックスサーバーにログイン
   https://secure.xserver.ne.jp/xapanel/login/xserver/
2. 「ドメイン」→「DNS レコード設定」→ `hiroseshinryojo.jp` を選択
3. 「DNS レコード一覧」のスクリーンショットを撮って Claude に見せる
4. その内容を確認してから、Vercel が指定する値に該当行だけ慎重に変更する
   - 例）A: `76.76.21.21` ／ CNAME（www）: `cname.vercel-dns.com`
   - ※ Vercel の**正確な指定値**は Vercel ダッシュボードの
     `Project → Settings → Domains` で確認する

## ユーザーの状況
- エックスサーバーにはログインできる。
- 独自ドメインのメールを使っているかは未確認 → だから MX は温存する。
