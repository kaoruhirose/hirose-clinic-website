<!-- BEGIN:nextjs-agent-rules -->
# This is NOT the Next.js you know

This version has breaking changes — APIs, conventions, and file structure may all differ from your training data. Read the relevant guide in `node_modules/next/dist/docs/` before writing any code. Heed deprecation notices.
<!-- END:nextjs-agent-rules -->

<!-- BEGIN:kaoru-common-rules v3 -->
# 共通ルール（全プロジェクト共通・廣瀬薫）

> **このブロックは、AGENTS.md を読むすべてのAIコーディングツールで機能します。**
> （Claude Code / OpenAI Codex / Cursor / GitHub Copilot / Gemini CLI など）
> 特定のツールに依存しません。ツールを乗り換えても、このファイルがそのまま次のツールで機能します。
>
> 全プロジェクトに同じ内容が入っています。**正本は OCEAN の深層**（`~/Projects/active/ocean/sea/deep/way/common-rules.md`）。
> 更新するときは正本を直して版番号を1つ上げ、`~/Projects/sync-common-rules.sh --apply` で全プロジェクトへ配ること
> （`--apply` を付けずに実行すると、書き換えずに差分だけ確認できる）。
> 新しいプロジェクトを始めるときは `~/Projects/_template/` をコピーしてください。

---

## 🚩 セッション開始時に必ず最初にやること

**どのツールを使っていても、会話の冒頭でこれを行う。ツール固有の記憶機能に頼らない。**

1. **同じフォルダにある `MAP.md`（作業日誌）を読む。**
2. **`MAP.md` が無ければ、本ファイル末尾の雛形をもとに新規作成する。**
3. 読んだ内容から、次を**日本語で**報告する：
   - 前回いつ、何をしたか
   - **やり残した宿題は何か**
4. そのうえで「今日は何をしますか？」と尋ねる。
5. **今日やることを聞いたら、着手する前に「どう進めるか」を一言で宣言する。**
   - 小さい・取り返しがつく → **そのままやる**（点数付けや見積りは省く）
   - まとまった実装や改修 → **型を起動する**（小分け → 点数付け → やり方を提示 → 検収）
   - 構想がぼんやりしている → **まず壁打ちする**（作らない。質問に徹する）

   宣言は1行でよい。目的は「毎回かならず一度立ち止まる」こと。判断の基準は下の「作業の進め方」。

> なぜこうするか：薫さんは前回の作業内容や宿題を **忘れる前提** で運用している。
> 覚えておく責任を人間ではなく `MAP.md` に持たせることで、
> 「どこまで何をしたか分からない」状態を構造的になくす。
> AI 側のセッション記憶は、ツールが変われば失われる。**記録はファイルに残す。**

---

## 相手について

作業相手は **廣瀬薫**。医師（漢方医）であり、**バイブコーディングの初心者**。

- **日本語で応答する。** ルールも記録も日本語で書く。
- **専門用語を使ったら、その場で日本語に言い換える。** 用語を知っている前提で話さない。
- **薫さんにターミナル（黒い画面）を触らせない。** コマンド実行が必要なら AI が自分で実行する。
  手順を渡して丸投げしない。
- 分からないことを質問しやすい雰囲気を保つ。判断を迫るときは選択肢と推奨を示す。

## 作業中のルール

- **1セッション＝1タスク。** 別のタスクに移るときは、いったん区切って新しいセッションにする。
  （長い会話は、毎回すべての履歴を読み直すため、費用がかさみ、文脈が混ざって精度が落ちる）
- **いきなり作り始めない。** 方針に迷いがある段階では、まず言葉で確認を取る。
  薫さんが「手を動かさないで」と言ったら、コードを書かずに相談だけする。
- **消す・上書きする前に、必ず中身を確認して報告する。** 削除は「ゴミ箱へ移動」を優先（戻せるように）。
- 作業が長くなったら、区切りごとに保存を提案する。

## 🎼 作業の進め方（判断の型）

**役割分担** — 賢い（高価な）AIは**判断**に使う：設計・危険の見抜き・検収。コードや調査の物量は、速い（定額の）AIに流す。使うツールが変わっても、この役割分担は変わらない。

**着手前に必ず止まる4場面** — ①設計の大きな決定 ②取り返しのつかない操作（削除・上書き・本番のデータ） ③外部への公開（デプロイ・公開リポジトリ） ④お金の投入。勝手に進めず、必ず薫さんに確認する。

**仕事の分け方** — まとまった作業は「1回で検収まで終わる単位」に小分けする。各単位に合格条件を **「この命令を実行 → こうなれば合格」の形で3〜7個**書く。「正しく動くこと」のような、確かめようのない条件は禁止。

**リスクの点数付け**（着手前に暗算する）

> お金が動く **+2** ／ 個人情報に触る **+2** ／ 取り返しがつかない **+2** ／ 影響が広い **+1** ／ いつもの定型作業 **−2**
>
> **4点以上**＝設計書から書く ／ **2〜3点**＝指示書と検収 ／ **0〜1点**＝自走＋検収 ／ **−1点以下**＝そのままやる
>
> 迷ったら必ず点数が高いほう（安全側）に倒す。

**検収3点セット（どんなに小さくても削らない）** — ①合格条件を、報告を読むのではなく**自分で実行して**確かめる ②変更箇所を目で見る ③出来上がったものを実際に読み、**「これを悪用するなら？ 途中で失敗したら？」**を自問する。新しくファイルや設定を作ったときは、パスワードや鍵が平文で書かれていないか（説明文書も含めて）必ず検索する。

**行き詰まったとき** — 同じやり方で3回目を試すのが一番高くつく。**2回失敗したら賢いAIへ上げる**。逆に、一度解けて手順が固まった仕事は、次から安いAIへ下げる。迷ったら安いほうから。

**構想がぼんやりしているとき** — いきなり作らない。まず質問で目的と理由をはっきりさせ、A4半分〜1枚にまとめてから着手する。このとき解決案や技術の話はしない — **良い質問をするのが仕事で、良い答えを出すのは仕事ではない。**

**型を呼ばない作業** — 30分以内で終わり・取り返しがつき・お金と個人情報に触らない作業は、上の手順を省いてそのままやってよい。**ただし検収3点セットだけは、規模によらず必ず行う。**

> このセクションは**1つ足すなら1つ削る**。長くなること自体が、この考え方への違反になる。
> 詳しい手順は `~/Projects/active/fable-orchestra/`（つかちゃんの公開リポジトリの複製。**読み取り専用**）。
> 読めるなら読む。**読めない環境なら、このページだけで判断してよい。**

## 🏁 セッション終了時にやること

薫さんが「今日の作業を保存して」と言ったら：

1. `MAP.md` の見本ブロックの直下に、今日の記録を追記する（**新しいものが上**）
   - 日付／やったこと／結果／**やり残した宿題**
2. `git add` → `git commit` → `git push` を実行する
3. 完了を報告し、新しいセッションを始めるよう伝える

## 環境の前提（迷子にならないための地図）

```
            ☁️ GitHub ← ここが「本当の保管場所」
                 │
      ┌──────────┴──────────┐
      │                     │
💻 MacBook Pro          🖥️ Mac mini（HIROSESHINRYOJO）
~/Projects/active/      ~/Projects/active/
                        ※ 外付けSSD接続済み
                        ※ Tailscale で外出先からも接続可
```

### 守るルール

1. **プロジェクトは必ず `~/Projects/active/` に置く。** 引退したものは `~/Projects/archive/` へ。
2. **`~/Desktop`・`~/Documents`・`~/Downloads` にプロジェクトを置かない。**
   macOS の保護機能により、自動起動したプログラムがアクセスできず失敗する
   （実例：Secretary Bot の常駐が失敗し `~/secretary-bot` へ移設して解決）。
3. **ホームフォルダ直下を作業場所にして AI を起動しない。**
   2026-04-07 に誤って `~/.git` が作られ、7.7GB のゴミが発生した（2026-08-27 に削除済み）。
4. **月に1回 `~/.git` が存在しないか確認する。** あれば即調査。
5. 2台の Mac は同じ形（`~/Projects/active/`）に揃える。片方だけ独自の形にしない。

## ファイルの役割分担

| ファイル | 役割 | 備考 |
|---|---|---|
| **`AGENTS.md`** | **ルールの本体。** 共通ルール＋プロジェクト固有 | 各社AIツール共通の標準。**これが正本** |
| `MAP.md` | 作業日誌 | 読ませる指示は `AGENTS.md` が持つ |
| `CLAUDE.md` | `@AGENTS.md` の1行のみ | Claude Code 用の橋渡し。不要になれば削除可 |

**ルールを書き換えるときは `AGENTS.md` を直す。** `CLAUDE.md` には中身を書かない。

他のツールが独自のルールファイル名を要求する場合も、同じ方針を取る
（中身は書かず、`AGENTS.md` を読むよう1行で指示するだけ）。

## 用語のかんたん対応表

| 用語 | 意味 |
|---|---|
| リポジトリ | プロジェクト1個分のフォルダ |
| コミット | 「この状態を記録する」＝ゲームのセーブ |
| プッシュ | セーブしたものを GitHub へ送る |
| セッション | AI との一続きの会話 |
| ロールバック | 過去のセーブ地点まで巻き戻すこと |

---

## `MAP.md` の雛形（無い場合はこれを使って作る）

````markdown
# MAP.md — 作業日誌

> **AI へ：** セッション開始時、必ずこのファイルを最初に読み、
> 「前回の作業内容」と「やり残した宿題」を日本語で報告してから作業を始めること。
> セッション終了時は、この見出しのすぐ下に新しい日付ブロックを追記する（**新しいものが上**）。

### 書き方の見本

## YYYY-MM-DD（曜）タイトル

### やったこと
- 箇条書き

### 結果
- 箇条書き

### 🔖 やり残した宿題
- [ ] 箇条書き（終わったら [x] にする）

---
````

<!-- END:kaoru-common-rules -->

# このプロジェクト固有

## 何をするサイトか

**廣瀬診療所のホームページ**（完全予約制の個人クリニック）。日本語サイト。
公開予定ドメイン：`hiroseshinryojo.jp`

## 技術構成

- Next.js 16（App Router。Pages Router ではない）/ React 19 / TypeScript 5（strict）
- Tailwind CSS 4 — 色の定義は `src/app/globals.css` の `@theme {}` ブロック（`tailwind.config.js` ではない）
- Framer Motion 12（スクロール連動アニメーション）/ Lucide React（アイコン）
- パス別名：`@/*` → `./src/*`
- GitHub: `kaoruhirose/hirose-clinic-website`

## よく使うコマンド

```bash
npm run dev      # 開発サーバー起動
npm run build    # 本番用ビルド
npm run start    # 本番サーバー起動
npm run lint     # コードのチェック
```

## ページ構成

すべて `src/app/` 以下（App Router の書き方）：

| ファイル | 役割 |
|---|---|
| `layout.tsx` | 全ページ共通の枠。`<Header>` と `<Footer>` を囲む |
| `page.tsx` | トップページ |
| `about/page.tsx` | 診療所について |
| `services/page.tsx` | 診療内容 |
| `access/page.tsx` | アクセス |

共通部品は `src/components/`（`Header.tsx` / `Footer.tsx`）。

## デザインの決まりごと

色は `src/app/globals.css` の `@theme {}` で定義済み：

| 名前 | 色 | 用途 |
|---|---|---|
| `clinic-base` | #f7f5f0 | 背景（あたたかいベージュ） |
| `clinic-blue` | #1c3c5a | 主色（濃い青） |
| `clinic-green` | #2a5d3e | 副色（深緑） |
| `clinic-text` | #2d3748 | 本文の文字色 |

フォントは `layout.tsx` で Google Fonts から読み込み：
**Zen Kaku Gothic New**（ゴシック体）と **Shippori Mincho**（明朝体）。
Tailwind の `font-sans` / `font-serif` で使い分ける。

アニメーションは Framer Motion の `useInView` + `motion.div` でスクロール連動。
新しく追加するときも既存ページと同じ書き方に合わせること。
